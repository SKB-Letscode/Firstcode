# Use pre-built image from Docker Hub
# This makes deployments 10x faster!
# Replace 'yourusername' with your Docker Hub username
FROM yourusername/fmf-api:latest

# Copy only the application code (dependencies already in base image)
COPY app ./app
COPY DB ./DB
COPY Images ./Images

# Expose port
EXPOSE 8000

# Start command
CMD ["uvicorn", "app.server.api_services_minimal:service", "--host", "0.0.0.0", "--port", "8000"]
